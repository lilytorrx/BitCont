import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    cabecalhoPreco: {
        width: "100%",
        height: "auto",
        alignItems: "center",
        marginBottom: 20
    },
    precoAtual: {
        color: "#2B73F0",
        fontSize: 28,
        fontWeight: "bold",
        paddingTop: 20
    },
    textoPreco: {
        color: "#fff",
        fontSize: 16,
        fontWeight: "bold"
    }
})

export default styles